import { MdInputBlurEventDirective } from './md-input-blur-event.directive';

describe('MdInputBlurEventDirective', () => {
  it('should create an instance', () => {
    const directive = new MdInputBlurEventDirective();
    expect(directive).toBeTruthy();
  });
});
